if ( -e ~/.aliases) then
    source ~/.aliases
endif

alias df "df -h"
alias wget 'wget -c'
alias ll 'ls -ahl'
alias rm 'rm -r'
alias la 'ls -a'
alias cp 'cp -r'

set autolist = ambiguous
set complete = enhance
